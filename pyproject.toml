[tool.poetry]
name = "kociemba"
version = "0.1.0"
description = ""
authors = ["Name <fake-noreply@email.com>"]
license = "GPLv3"

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
